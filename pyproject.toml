[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gitops-oncall-mcp"
version = "0.1.0"
description = "MCP server giving an LLM agent a typed, auditable on-call surface over Kubernetes, Argo CD, Argo Rollouts, Prometheus, Loki, Tempo and GitHub."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Saputra Uta" },
]
keywords = ["mcp", "model-context-protocol", "kubernetes", "argocd", "gitops", "sre", "on-call", "observability", "ai-agent", "claude"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Monitoring",
    "Topic :: System :: Systems Administration",
]
dependencies = [
    "fastmcp>=3.3.0",
    "httpx>=0.27.0",
    "kubernetes>=31.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.6.0",
    "mypy>=1.10.0",
    "respx>=0.21.0",
]

[project.scripts]
gitops-oncall-mcp = "gitops_oncall_mcp.__main__:main"

[project.urls]
Homepage = "https://github.com/SaputraUta/gitops-oncall-mcp"
Issues = "https://github.com/SaputraUta/gitops-oncall-mcp/issues"
Repository = "https://github.com/SaputraUta/gitops-oncall-mcp"

[tool.hatch.build.targets.wheel]
packages = ["src/gitops_oncall_mcp"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
